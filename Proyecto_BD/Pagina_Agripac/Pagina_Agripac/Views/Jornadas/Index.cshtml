@model IEnumerable<Pagina_Agripac.Jornada>

@{
    ViewBag.Title = "Index";
}

<h2>Jornadas</h2>

<p>
    @Html.ActionLink("Crear Registro", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Jornada1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Jornada1)
        </td>
        <td>
            @Html.ActionLink("✏", "Edit", new { id=item.id_jornada }) |
            @Html.ActionLink("👀", "Details", new { id=item.id_jornada }) |
            @Html.ActionLink("❌", "Delete", new { id=item.id_jornada })
        </td>
    </tr>
}

</table>
