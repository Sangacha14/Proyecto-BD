@model IEnumerable<Pagina_Agripac.Departamento>

@{
    ViewBag.Title = "Index";
}

<h2>Departamentos</h2>

<p>
    @Html.ActionLink("Crear Departamento", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Departamento1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cargo.Cargo1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Departamento1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Cargo.Cargo1)
        </td>
        <td>
            @Html.ActionLink("✏", "Edit", new { id=item.idDepartamento }) |
            @Html.ActionLink("👀", "Details", new { id=item.idDepartamento }) |
            @Html.ActionLink("❌", "Delete", new { id=item.idDepartamento })
        </td>
    </tr>
}

</table>
