@model IEnumerable<Pagina_Agripac.Motivo_Asistencia>

@{
    ViewBag.Title = "Index";
}

<h2>Motivo de Inasistencia</h2>

<p>
    @Html.ActionLink("Crear Nuevo Registro", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Motivo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Motivo)
        </td>
        <td>
            @Html.ActionLink("✏", "Edit", new { id=item.idMotivo_Asistencia }) |
            @Html.ActionLink("👀", "Details", new { id=item.idMotivo_Asistencia }) |
            @Html.ActionLink("❌", "Delete", new { id=item.idMotivo_Asistencia })
        </td>
    </tr>
}

</table>
